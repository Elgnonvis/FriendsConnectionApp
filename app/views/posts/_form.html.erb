<%= simple_form_for(post, local:true, url: new_or_create_not_edit) do |f| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="form-inputs">
      <%= image_tag(@post.illustration.url) if @post.illustration && @post.illustration.url %>
      <%= f.input :illustration, as: :file %><br>
      <%= f.hidden_field :illustration_cache %>
      <br>
      <%= f.input :description %>
    </div><br>
  <div class="form-actions">
    <%= f.button :submit, "Publier", class: 'btn-primary' %>
  </div>
  
<% end %>
